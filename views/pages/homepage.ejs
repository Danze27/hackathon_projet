<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bibli Handi</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/style.css">
    <style>
        body {
    font-size: 1.1rem;
}

:focus {
    outline: 3px solid orange;
    outline-offset: 3px;
}

    </style>
</head>
<body class="bg-light text-dark">

    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="#">📚 Bibli Handi</a>
        </div>
    </nav>

    <main class="container mt-5">
        <h1 class="mb-4">Bienvenue sur Bibli Handi</h1>
        <p>Consultez facilement des documents accessibles à tous.</p>

        <div class="mb-3">
            <label for="search" class="form-label">Rechercher un document :</label>
            <input type="text" id="search" class="form-control" placeholder="Titre, format, accessibilité...">
        </div>

        <div class="row">
            <% documents.forEach(doc => { %>
                <div class="col-md-4 mb-4">
                    <div class="card h-100 shadow-sm">
                        <div class="card-body">
                            <h5 class="card-title"><%= doc.titre %></h5>
                            <p class="card-text">
                                <strong>Format :</strong> <%= doc.format %><br>
                                <strong>Accessibilité :</strong> <%= doc.access %>
                            </p>
                            <a href="#" class="btn btn-primary">Lire / Écouter</a>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </main>

    <footer class="bg-dark text-white text-center py-3 mt-5">
        <small>Site conçu pour l’accessibilité – Navigation au clavier, contrastes renforcés, etc.</small>
    </footer>

</body>
</html>
